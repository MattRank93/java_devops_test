#logging
spring:
  application:
    name: demo1
  data:
    mongodb:
      host: localhost
      port: 27017
      database: SSSS
  profiles:
    active: dev



#rabbitmq:
#  image: rabbitmq:management
#  ports:
#    - "5672:5672"
#    - "15672:15672"

server:
  port: 7885

logging:
  level:
    org:
      springframework: debug
  file:
    name: ${java.io.tmpdir}/${spring.application.name}.log
